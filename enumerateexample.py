for index, character in enumerate("abcdefgh"): #nothing special about the variable names
    print(index, character)
    # we are getting the output of the idex positions and their matching characters
    #enumetate function is built into python
